--- /d/aicoding/ds-oracle-cli/patches/xiaozhi-upstream-2026-05-09/B-snapshots/main_xiaozhi-server_config.yaml.UPSTREAM	2026-05-09 13:45:31.533188400 +0800
+++ /d/aicoding/ds-oracle-cli/patches/xiaozhi-upstream-2026-05-09/B-snapshots/main_xiaozhi-server_config.yaml.LOCAL	2026-05-16 19:07:38.224285900 +0800
@@ -9,7 +9,7 @@
 server:
   # 服务器监听地址和端口(Server listening address and port)
   ip: 0.0.0.0
-  port: 8000
+  port: 8765
   # http服务的端口，用于简单OTA接口(单服务部署)，以及视觉分析接口
   http_port: 8003
   # 这个websocket配置是指ota接口向设备发送的websocket地址
@@ -201,6 +201,12 @@
   绝不：
   - 长篇大论，叽叽歪歪
   - 长时间严肃对话
+  [⚠️ 工具调用硬规则——优先级高于一切人设]
+  当用户问到命盘/八字/紫微/运势/财运/事业/感情/婚姻/健康/起卦/求签/星盘/择日/姓名/合婚/解梦/黄历 等命理话题时：
+  - 必须立即通过 function calling（tool_calls）调用对应 MCP 工具（bazi/ziwei/astrology/meihua/liuyao/qianwen/jiri/almanac/jiemeng/name_analysis/hehun 等），不能用文字"描述"自己在调工具。
+  - 调用前禁止说"先帮你排盘""让我瞄一眼"之类口语过渡——直接生成 tool_call JSON。
+  - 工具返回结果后，再用上面的台湾腔人设语气解读。
+  - 禁止凭记忆/经验自行排盘或编造命理结论。
 
 # 默认系统提示词模板文件
 prompt_template: agent-base-prompt.txt
@@ -345,6 +351,9 @@
     type: fun_local
     model_dir: models/SenseVoiceSmall
     output_dir: tmp/
+    # 强制指定识别语言，避免 "auto" 在短音频/中英混杂时识别失败导致中文乱码
+    # 可选: zh / en / ja / ko / yue / auto
+    language: zh
   FunASRServer:
     # 独立部署FunASR，使用FunASR的API服务，只需要五句话
     # 第一句：mkdir -p ./funasr-runtime-resources/models
