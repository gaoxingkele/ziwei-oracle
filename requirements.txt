# ── 玄学算法库：pin 死已验证版本，防云端重装版本漂移导致排盘结果变化 ──
kerykeion==5.12.8
kinqimen==0.0.6.6
kinliuren==0.1.2.9
ichingshifa==3.1.9
cn2an>=0.5
najia==2.0.1
openai>=1.0
Pillow>=10.0
python-dotenv>=1.0
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0
pydantic>=2.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
aiosqlite>=0.19
alembic>=1.13
redis[hiredis]>=5.0
pyjwt>=2.8
httpx>=0.27
pytest>=8.0
pytest-asyncio>=0.23
mcp>=1.0
# 注: lunar_python 已内联到 app/lunar/ (PyPI 最新仅到 1.4.8 不满足历史 >=1.7
# 写法, 且项目代码已不依赖 pip 包). najia 同理已内联到 app/najia/, requirements
# 里的 najia 是上游版用作 fallback.
